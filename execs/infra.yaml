metadata:
  name: Montjoie tests
  format: "Lava-Test-Shell Test Definition 1.0"
  description: "Verify LAVA network"
  version: 1.0
run:
    steps:
      - sleep 10
      - echo "Network tests"
      - lava-test-set start test-network
      - chmod +x ./infra.sh
      - ./infra.sh
      - lava-test-set stop test-network
